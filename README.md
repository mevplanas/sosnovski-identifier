# Sosnovski identifier 

The codes are in the `sosnovski.ipynb` file.

# Creating the env 

```bash
python3.11 -m venv venv
source venv/bin/activate # Linux 
venv\Scripts\activate # Windows
pip install -r requirements.txt
```